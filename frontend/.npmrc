# Supply-chain hardening for pnpm
# TEMP: install-time soak disabled while landing a Renovate PR backlog.
# Restore by uncommenting the line below (intended value: 10080 = 7 days).
# minimum-release-age=10080
prefer-frozen-lockfile=true
save-exact=true
audit-level=moderate
